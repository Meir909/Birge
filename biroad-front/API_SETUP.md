# Настройка API ключей для BIRoad

## 📋 Обзор

Для полноценной работы фронтенда необходимо настроить API ключи для различных сервисов. Этот файл содержит подробные инструкции по получению и настройке всех необходимых ключей.

## 🔑 Необходимые API ключи

### 1. Google Maps API

**Для чего нужен:**
- Интерактивные карты
- Геокодирование (адреса ↔ координаты)
- Построение маршрутов
- Поиск nearby мест

**Как получить:**
1. Перейдите в [Google Cloud Console](https://console.cloud.google.com/)
2. Создайте новый проект или выберите существующий
3. Включите следующие API:
   - Maps JavaScript API
   - Geocoding API
   - Directions API
   - Places API
4. Создайте API ключ в разделе "Credentials"
5. Настройте ограничения для ключа:
   - HTTP-рефереры: `http://localhost:3000/*`, `https://yourdomain.com/*`
   - API ограничения: выберите только необходимые API

**Настройка в config.js:**
```javascript
MAPS: {
    GOOGLE_MAPS_API_KEY: 'ВАШ_GOOGLE_MAPS_API_KEY',
    // ...
}
```

### 2. OpenAI API

**Для чего нужен:**
- Умные рекомендации маршрутов
- Анализ безопасности
- Эко-рекомендации
- Чат-бот поддержки

**Как получить:**
1. Перейдите на [platform.openai.com](https://platform.openai.com/)
2. Зарегистрируйтесь или войдите
3. Перейдите в раздел "API Keys"
4. Создайте новый секретный ключ
5. Скопируйте ключ (он будет показан только один раз)

**Настройка в config.js:**
```javascript
AI: {
    OPENAI_API_KEY: 'sk-ВАШ_OPENAI_API_KEY',
    // ...
}
```

### 3. Here Maps API (альтернатива)

**Для чего нужен:**
- Альтернативный провайдер карт
- Трафик в реальном времени
- Маршрутизация

**Как получить:**
1. Зарегистрируйтесь на [developer.here.com](https://developer.here.com/)
2. Создайте новый проект
3. Сгенерируйте API ключ
4. Включите необходимые сервисы

**Настройка в config.js:**
```javascript
GEO: {
    HERE_API_KEY: 'ВАШ_HERE_API_KEY',
    // ...
}
```

### 4. OpenWeatherMap API

**Для чего нужен:**
- Погодные условия
- Прогноз для планирования поездок

**Как получить:**
1. Зарегистрируйтесь на [openweathermap.org](https://openweathermap.org/api)
2. Получите бесплатный API ключ
3. Подтвердите email для активации

**Настройка в config.js:**
```javascript
WEATHER: {
    OPENWEATHER_API_KEY: 'ВАШ_OPENWEATHER_API_KEY',
    // ...
}
```

## 🚀 Быстрая настройка

### Шаг 1: Копирование файла конфигурации

```bash
cp js/config.js js/config.local.js
```

### Шаг 2: Редактирование config.local.js

Замените все `'YOUR_..._API_KEY_HERE'` на реальные ключи:

```javascript
// Пример для Google Maps
GOOGLE_MAPS_API_KEY: 'AIzaSyBxxxxxxxxxxxxxxxxxxxxxxxxxxx',

// Пример для OpenAI
OPENAI_API_KEY: 'sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx',
```

### Шаг 3: Использование локальной конфигурации

Измените порядок загрузки в HTML файлах:

```html
<script src="js/config.local.js"></script>
<script src="js/config.js"></script>
```

## 🛡️ Безопасность

### Никогда не храните API ключи в:
- Публичных репозиториях
- Клиентском коде в продакшене
- JavaScript файлах без защиты

### Рекомендуемые подходы:

#### 1. Переменные окружения (Node.js + Express)
```javascript
// server.js
const config = {
    MAPS: {
        GOOGLE_MAPS_API_KEY: process.env.GOOGLE_MAPS_API_KEY
    }
};

// Передача в фронтенд
app.get('/api/config', (req, res) => {
    res.json({
        googleMapsApiKey: process.env.GOOGLE_MAPS_API_KEY
    });
});
```

#### 2. .env файл
```bash
# .env
GOOGLE_MAPS_API_KEY=AIzaSyBxxxxxxxxxxxxxxxxxxxxxxxxxxx
OPENAI_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
```

#### 3. Прокси-сервер
```javascript
// Создайте эндпоинт для API запросов
app.post('/api/ai/chat', async (req, res) => {
    const response = await fetch('https://api.openai.com/v1/chat/completions', {
        headers: {
            'Authorization': `Bearer ${process.env.OPENAI_API_KEY}`
        },
        body: JSON.stringify(req.body)
    });
    
    const data = await response.json();
    res.json(data);
});
```

## 🧪 Тестирование

### Проверка Google Maps
```javascript
// В консоли браузера
window.mapService.init().then(() => {
    console.log('Google Maps успешно загружены');
});
```

### Проверка OpenAI
```javascript
// В консоли браузера
window.aiService.sendRequest([
    { role: 'user', content: 'Привет!' }
]).then(response => {
    console.log('OpenAI работает:', response);
});
```

## 💰 Стоимость API

### Бесплатные лимиты:

**Google Maps:**
- $200 кредит в месяц бесплатно
- ~28,600 загрузок карт
- ~100,000 геокодирований

**OpenAI:**
- $5 кредит при регистрации
- gpt-3.5-turbo: $0.002/1K токенов
- gpt-4: ~$0.03/1K токенов

**OpenWeatherMap:**
- 60 вызовов в минуту
- 1,000,000 вызовов в месяц бесплатно

## 🔧 Устранение проблем

### Ошибка "Google Maps API key invalid"
- Проверьте правильность ключа
- Убедитесь что API включены в Google Cloud Console
- Проверьте ограничения HTTP-рефереров

### Ошибка "OpenAI API key invalid"
- Убедитесь что ключ начинается с "sk-"
- Проверьте баланс аккаунта
- Убедитесь что API не заблокирован

### Ошибка CORS
- Настройте CORS на бэкенде
- Используйте прокси-сервер для API запросов

## 📱 Мобильная версия

Для мобильных приложений используйте:
- Google Maps SDK для Android/iOS
- Platform-specific OpenAI SDK
- Нативные геолокационные API

## 🚀 Продакшен развертывание

### 1. Сборка переменных окружения
```bash
# production.env
GOOGLE_MAPS_API_KEY=AIzaSyBxxxxxxxxxxxxxxxxxxxxxxxxxxx
OPENAI_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
```

### 2. Настройка CDN
- Разместите карты на CDN
- Используйте кэширование для API ответов

### 3. Мониторинг
- Отслеживайте использование API
- Настраивайте алерты при превышении лимитов

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте консоль браузера на ошибки
2. Убедитесь что все API ключи правильные
3. Проверьте сетевые запросы во вкладке Network
4. Свяжитесь с поддержкой соответствующего сервиса

---

**Важно:** Всегда следуйте политике использования API ключей и не злоупотребляйте лимитами!
